apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dave-adapter-telraam.fullname" . }}-test-connection"
  labels:
    {{- include "dave-adapter-telraam.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dave-adapter-telraam.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
