apiVersion: v1
kind: Service
metadata:
  name: {{ include "dave-adapter-telraam.fullname" . }}
  labels:
    {{- include "dave-adapter-telraam.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dave-adapter-telraam.selectorLabels" . | nindent 4 }}
